const path = require('path');

module.exports = {
  'config': path.resolve('src/Infrastructure/config', 'sequelize-config.json'),
  'migrations-path': path.resolve('migrations'),
  'seeders-path': path.resolve('seeders'),
};
